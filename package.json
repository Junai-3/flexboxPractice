{
  "name": "node-starter",
  "private": true,
  "scripts": {
    "start": "serve .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "express": "^5.1.0",
    "server": "^1.0.41"
  }
}
